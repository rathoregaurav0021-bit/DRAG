def simulate_discharge(precipitation_data: list) -> float:
    \"\"\"
    Placeholder for wflow hydrology integration.
    Converts rainfall into river discharge.
    \"\"\"
    # Mock logic: sum of precipitation * coefficient
    total_rain = sum(precipitation_data)
    discharge = total_rain * 1.5 
    return discharge
